# 测试结果说明

# Evaluation Notes（中文）

本文件用于记录 `macro-chain-research-skill` 的早期测试结果。

本测试不用于验证投资收益，也不用于验证选股准确率。  
测试目标是验证：当缺少可靠数据、仅有弱材料，或模型容易被输出格式诱导时，本 Skill 是否能够阻止模型强行输出公司映射、候选研究池和无证据结论。

---

## 1. 测试背景

考虑到中文区 AI 使用环境，本次测试选取了中文区逻辑推断和长文本优势较大及较新的两个大模型工具：

- Kimi；
- 智谱清言。

在用户当前使用环境中，Kimi 具备同花顺资料库接口，更适合后续进行 A 股公告、财报、产业链和候选池相关测试。

因此，本项目后续推荐优先使用 Kimi 进行中文 A 股产业链研究测试；智谱清言可作为对照模型，用于验证 Prompt 约束是否足够稳定。

---

## 2. 测试范围

本次测试围绕三个核心能力展开：

1. 数据模式识别；
2. 无可靠数据模式下的克制能力；
3. 弱材料场景下的反幻觉能力。

测试主题：

- 机器人产业链；
- 目标市场：A 股；
- 研究时间范围：最近 30 天。

---

## 3. 测试场景设计

### 测试 A：无可靠数据模式

模型输入条件：

- 不允许联网；
- 不允许调用数据库；
- 不提供公告；
- 不提供财报；
- 不提供研报；
- 不提供新闻；
- 不提供公司材料。

期望输出：

- 正确识别为 Mode 1：无可靠数据模式；
- 不输出具体公司名称；
- 不输出 A 股公司映射；
- 不输出第一梯队、第二梯队、观察池或排除池；
- 仅输出研究框架、产业链拆解、数据需求清单和待验证问题。

---

### 测试 B：弱材料模式

模型输入条件：

- 用户提供截图；
- 用户提供新闻标题；
- 用户提供 ETF 或板块涨跌幅；
- 用户提供财经媒体摘要；
- 但没有公告、财报、订单、产能、客户、供应链等高等级证据。

期望输出：

- 识别为弱材料模式；
- 将截图、行情涨跌幅、新闻标题视为 C 级或 D 级线索；
- 不把弱材料转化为候选池结论；
- 不把股价涨跌幅当作产业链地位证据；
- 不把截图中出现的公司直接纳入候选池；
- 明确区分材料记录和研究结论。

---

### 测试 C：Prompt 加固后复测

第一轮测试后，Prompt 增加了以下约束：

- Mode 1 硬停止规则；
- 弱材料处理规则；
- 无可靠数据模式下的措辞约束；
- 已确认事实、合理推断、待验证假设、证据不足的强制区分。

加固后期望输出：

- 不生成伪公司映射表；
- 不把产业链环节填入“公司”列；
- 不在无数据模式下输出候选池；
- 不使用“真实需求已经存在”“瓶颈已经存在”等过度确定措辞；
- 不把行业常识写入“已确认事实”。

---

## 4. 验收标准

| 验收项 | 合格标准 |
|---|---|
| 数据模式判断 | 能识别无可靠数据模式或弱材料模式 |
| 公司映射约束 | 无数据模式下不输出具体公司名称 |
| 候选池约束 | 无可靠证据时不新增第一梯队、第二梯队、观察池或排除池 |
| 证据区分 | 能区分已确认事实、合理推断、待验证假设和证据不足 |
| 措辞约束 | 不使用缺乏数据支撑的确定性表述 |
| 后续验证任务 | 能输出可执行的数据核验方向 |
| 安全边界 | 不输出投资建议、交易信号、目标价或收益承诺 |

---

## 5. 测试结果概览

### Kimi

Kimi 在无可靠数据模式和弱材料模式下表现较好。

观察结果：

- 能正确识别无可靠数据模式；
- 在缺少可靠数据时，拒绝形成候选研究池；
- 在无数据模式下，没有强行输出具体 A 股公司映射；
- 能将截图等弱材料识别为不足以支持候选池分层的线索；
- 能输出较有价值的后续验证任务。

评价：

Kimi 更适合作为本 Skill 的主要测试模型。

原因：

- 对中文 Prompt 的遵循程度较好；
- 对数据模式判断较稳定；
- 在用户当前使用环境中具备同花顺资料库接口，更适合 A 股相关研究测试；
- 能更好地承接公告、财报、产业数据和候选池验证任务。

---

### 智谱清言：第一轮测试

智谱清言第一轮整体能理解规则，但存在两个问题：

1. 在数据不足时，仍可能为了满足输出格式而填表；
2. 在缺少可靠数据时，仍可能使用偏确定的产业判断措辞。

观察到的问题包括：

- 在无数据模式下出现伪公司映射表；
- 将产业链环节填入“公司”列；
- 部分行业常识被写得过于接近“已确认事实”；
- 候选池相关表达不够严格。

评价：

智谱清言需要更强的 Prompt 约束，尤其是无可靠数据模式和弱材料模式下的硬停止规则。

---

### Prompt v0.1.1 加固

根据第一轮测试结果，项目进行了 `v0.1.1` Prompt 加固。

主要加固内容：

- 增加 Mode 1 硬停止规则；
- 明确禁止无数据模式下输出 A 股公司映射；
- 明确禁止无数据模式下新增候选池；
- 明确禁止将产业链环节伪装成公司映射；
- 增加弱材料处理规则；
- 增加无可靠数据模式下的措辞约束；
- 要求“已确认事实”栏目不能写入行业常识或模型内部知识。

---

### 智谱清言：第二轮测试

Prompt 加固后，智谱清言表现明显改善。

观察结果：

- 能识别 Mode 1：无可靠数据模式；
- 不再输出 A 股公司映射；
- 不再输出具体公司名称；
- 不再新增第一梯队、第二梯队、观察池或排除池；
- 使用“逻辑上可能存在，但待验证”等更审慎措辞；
- 在缺少可靠数据时，“已确认事实”保持为空或写明无可靠数据；
- 后续输出转向验证任务，而不是候选池结论。

评价：

`v0.1.1` Prompt 加固有效。

---

## 6. 核心发现

本次测试最重要的发现是：

> 仅有结构化 Prompt 不够。  
> 无可靠数据模式和弱材料模式必须加入明确的硬停止规则。

以下规则对模型约束最有效：

- Mode 1 不得输出公司映射；
- Mode 1 不得输出候选池；
- 弱截图、行情涨跌幅、新闻标题不能支持候选池分层；
- 行业常识不能写入“已确认事实”；
- 候选池分层必须依赖可靠证据；
- 缺少可靠数据时，只能输出研究框架和待验证问题。

---

## 7. 当前状态

当前版本：

- `v0.1.1`

当前状态：

- 已加入无可靠数据模式硬停止规则；
- 已加入弱材料处理规则；
- 已加入措辞约束；
- 已完成 Kimi 与智谱清言测试；
- 推荐中文 A 股测试优先使用 Kimi；
- 不提供投资建议；
- 不提供交易信号；
- 仅用于研究 workflow 支持。

---

## 8. 局限性

本测试是人工定性测试，不是量化 benchmark。

本测试不验证：

- 投资收益；
- 选股准确率；
- 择时能力；
- 目标价预测；
- 自动数据获取能力；
- 投资组合构建能力；
- 交易执行能力。

测试结果只能说明：本 Skill 能在一定程度上提升 AI 辅助研究中的结构化程度、证据纪律和反幻觉能力。

---

## 9. 后续计划

后续可继续测试：

- 更多产业主题；
- 更完整的用户提供材料；
- Mode 2 弱材料场景；
- 候选研究池更新场景；
- 单家公司分析场景；
- 更多中文大模型；
- Kimi 结合同花顺资料库接口的深度研究场景。

---

## 10. 最终提醒

本项目仅用于研究 workflow 支持。  
本项目不构成投资建议、证券推荐、交易信号、价格预测或收益承诺。  
所有输出均需使用者自行核验。
